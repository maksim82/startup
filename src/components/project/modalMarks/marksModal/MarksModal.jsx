import styles from './marksModal.module.scss';

import data from './data.json';

const MarksModal = ({handlePage}) => {   
    console.log(data);

    return (
        <div>
            <h2></h2>
        </div>
    )
}

export default MarksModal;